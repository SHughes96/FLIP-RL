from setuptools import setup
import numpy as np

setup(name='Delta_RL',
      version='0.0.1',
      install_requires=['gym',
                        'numpy'
                        ]  # And any other dependencies
)

